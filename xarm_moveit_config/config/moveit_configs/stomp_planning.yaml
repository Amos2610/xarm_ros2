planning_plugin: stomp_moveit/StompPlanner
# Humble の planning_pipeline は request_adapters だけを読む（response_adapters は Iron 以降）。
# adapter のクラス名も Humble のもの（default_planner_request_adapters/...）で書く。
# 名前が違うと 1 つも読み込まれず、開始状態の境界・衝突検査も時間パラメータ化も
# 無しで計画が通ってしまう。ompl_planning.yaml と同じ名前に揃える。
#
# FixStartStateCollision は「開始状態が衝突なら jiggle_fraction の範囲で開始状態を
# ずらして計画する」修正型の adapter。「ずらして通す」のではなく「止める」挙動に
# したいので jiggle_fraction を 0 にし、衝突していれば START_STATE_IN_COLLISION で
# 失敗させる。
start_state_max_bounds_error: 0.1
jiggle_fraction: 0.0
max_sampling_attempts: 1
request_adapters: >-
    default_planner_request_adapters/AddTimeOptimalParameterization
    default_planner_request_adapters/ResolveConstraintFrames
    default_planner_request_adapters/FixWorkspaceBounds
    default_planner_request_adapters/FixStartStateBounds
    default_planner_request_adapters/FixStartStateCollision
    default_planner_request_adapters/FixStartStatePathConstraints
stomp_moveit:
  num_timesteps: 60
  num_iterations: 40
  num_iterations_after_valid: 0
  num_rollouts: 30
  max_rollouts: 30
  exponentiated_cost_sensitivity: 0.5
  control_cost_weight: 0.1
  delta_t: 0.1
